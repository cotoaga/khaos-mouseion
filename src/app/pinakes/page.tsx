import PinakesView from '@/components/PinakesView';

export const metadata = {
  title: 'PINAKES — KHAOS-Mouseion',
  description: 'Catalog of Callimachus. 1,465 books, faceted by theme, category, and medium.',
};

export default function PinakesPage() {
  return <PinakesView />;
}
